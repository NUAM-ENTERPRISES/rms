# API Configuration
VITE_API_URL=http://localhost:3000/api/v1

# Introduction video upload
# false = upload through Nest API on localhost (no direct browser -> Spaces PUT)
# true  = presigned direct upload to DigitalOcean Spaces (requires bucket CORS)
VITE_INTRO_VIDEO_DIRECT_UPLOAD=false

# App Configuration
VITE_APP_NAME="Affiniks RMS"
VITE_APP_VERSION="1.0.0"
