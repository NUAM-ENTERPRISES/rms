// Feature-specific hooks for candidates
export {
  useGetCandidatesQuery,
  useGetCandidateByIdQuery,
  useCreateCandidateMutation,
  useUpdateCandidateMutation,
  useDeleteCandidateMutation,
  useAssignToProjectMutation,
  useNominateCandidateMutation,
  useApproveOrRejectCandidateMutation,
  useGetEligibleCandidatesQuery,
} from "./api";

// Additional feature-specific hooks can be added here
// For example: useCandidateFilters, useCandidateSearch, etc.
